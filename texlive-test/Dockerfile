# Use the texlive-latest container as the base image
FROM texlive/texlive:latest

# Set the working directory inside the container
WORKDIR /workspace

# Optional: Copy your local files into the container
# COPY . /workspace

# Start a shell when the container runs
CMD ["bash"]